# AIMAR Command Deck — Environment Variables
# Copy this file to .env.local for local development
# In Vercel: add these under Project Settings → Environment Variables

# Optional: Gemini API key for live AI features (ODSM Analyze + Workflow Synthesis)
# Without this key the app runs in DEMO MODE with deterministic mock responses
VITE_GEMINI_API_KEY=
